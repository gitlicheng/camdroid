
/*************************************************************************
 > File Name: MenuPhoto.h
 > Description:
 > Description:
 >	Generated by script
 ************************************************************************/

#ifndef __MENUPHOTO_H__
#define __MENUPHOTO_H__

#include <minigui/common.h>
#include <minigui/minigui.h>
#include <minigui/gdi.h>
#include <minigui/window.h>
#include <minigui/control.h>

#include "windows.h"
#include "cdr_widgets.h"
#include "keyEvent.h"
#include "cdr_message.h"
#include "StorageManager.h"
#include "ResourceManager.h"

#define MENU_LIST_COUNT  5

enum menuIndex {
	MENU_INDEX_PHOTO_RESOLUTION,
	MENU_INDEX_PHOTO_COMPRESSION_QUALITY,
	MENU_INDEX_WB,
	MENU_INDEX_EXPOSURE,
	MENU_INDEX_PHOTO_WM
};

static enum ResourceID menuResourceID[] = {
	ID_MENU_LIST_PHOTO_RESOLUTION,
	ID_MENU_LIST_PHOTO_COMPRESSION_QUALITY,
	ID_MENU_LIST_WB,
	ID_MENU_LIST_EXPOSURE,
	ID_MENU_LIST_PHOTO_WM
};

static unsigned int haveCheckBox[] = {
	0, 	/*PHOTO_RESOLUTION 		 not have check box */
	0, 	/*PHOTO_COMPRESSION_QUALITY 		 not have check box */
	0, 	/*WB 		 not have check box */
	0, 	/*EXPOSURE 		 not have check box */
	1 	/*PHOTO_WM 		 have check box */
};

static unsigned int haveSubMenu[] = {
	1, 	/*PHOTO_RESOLUTION 		 have sub menu */
	1, 	/*PHOTO_COMPRESSION_QUALITY 		 have sub menu */
	1, 	/*WB 		 have sub menu */
	1, 	/*EXPOSURE 		 have sub menu */
	0 	/*PHOTO_WM 		 not have sub menu */
};

static unsigned int haveValueString[] = {
	1, 	/*PHOTO_RESOLUTION 		 have value string */
	1, 	/*PHOTO_COMPRESSION_QUALITY 		 have value string */
	1, 	/*WB 		 have value string */
	1, 	/*EXPOSURE 		 have value string */
	0 	/*PHOTO_WM 		 not have value string */
};


static int subMenuContent0Cmd[] = {
	LANG_LABEL_SUBMENU_PHOTO_RESOLUTION_CONTENT0,
	LANG_LABEL_SUBMENU_PHOTO_COMPRESSION_QUALITY_CONTENT0,
	LANG_LABEL_SUBMENU_WB_CONTENT0,
	LANG_LABEL_SUBMENU_EXPOSURE_CONTENT0,
	-1 	/*PHOTO_WM not have sub menu */
};


static MLFlags mlFlags[] = {
/***** 1. image flag	2. value flags	3. value counts *****************/
	/*PHOTO_RESOLUTION*/
	{ IMGFLAG_IMAGE, {VMFLAG_STRING, VMFLAG_IMAGE}, 2},

	/*PHOTO_COMPRESSION_QUALITY*/
	{ IMGFLAG_IMAGE, {VMFLAG_STRING, VMFLAG_IMAGE}, 2},

	/*WB*/
	{ IMGFLAG_IMAGE, {VMFLAG_STRING, VMFLAG_IMAGE}, 2},

	/*EXPOSURE*/
	{ IMGFLAG_IMAGE, {VMFLAG_STRING, VMFLAG_IMAGE}, 2},

	/*PHOTO_WM*/
	{ IMGFLAG_IMAGE, {VMFLAG_IMAGE}, 1},

};


#endif

